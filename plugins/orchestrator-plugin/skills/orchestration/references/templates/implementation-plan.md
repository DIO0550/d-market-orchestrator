# {目的を簡潔に記述}

## 指示

タスクを分析し、具体的な実装計画を作成する。
Explorer の探索結果を入力として受け取り、タスク分割・依存関係定義・実装手順を出力する。

### 計画の原則
- **CLAUDE.md のルールが最優先** -- 計画に制約として反映し、Implementer に伝わるようにする
- **1タスク = 1つの明確な成果物** -- タスクが大きすぎる場合は分割する
- **依存関係は必須** -- 全タスクに `blockedBy` / `blocks` を定義する（独立タスクは `blockedBy: なし`）
- **TaskCreate / TaskUpdate ツールが利用可能な場合は積極的に使用する**

## 事前確認

1. `CLAUDE.md` を読み、プロジェクト固有のルール・禁止事項を把握する
2. Explorer の探索結果（`.orchestrator/exploration.md`）を読み、関連ファイル・パターンを把握する

## 出力フォーマット

**関連Issue**: #{Issue番号} <!-- Issueから作成した場合のみ -->

{背景・なぜこの変更が必要か、1-2文}

### ユーザーレビューが必要な点

> **IMPORTANT** / **NOTE**
> - {確認してほしい判断事項}
> - {破壊的変更の有無}
> - {設計上のトレードオフ}

### システム図

#### 状態マシン / フロー図

> 以下の図は省略不可。必ず具体的な内容で埋めること。

```
{状態マシン図 -- 全状態・遷移条件・分岐・エッジケース・ループを含む}
```

#### データフロー

> 以下の図は省略不可。必ず具体的な内容で埋めること。

```
{コンポーネント間のデータの流れ}
```

### 変更案

#### {カテゴリ: プロジェクト構造 / UI / ロジック / etc.}

##### [NEW] `{ファイルパス}`

{このファイルで何をするか}

##### [MODIFY] `{ファイルパス}`

{変更内容}

##### [DELETE] `{ファイルパス}`

{削除理由}

### 検証計画

#### 自動テスト

```bash
{実行するコマンド}
```

- {確認内容}

#### 手動検証

1. {具体的な確認手順}
